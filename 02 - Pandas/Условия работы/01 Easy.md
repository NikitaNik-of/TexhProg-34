# Практическая работа #2:

## Легенда
Вы устроились младшим аналитиком данных в сеть киберспортивных клубов. Менеджер выгрузил из биллинговой системы данные по сессиям за прошлый месяц. Ваша задача — выполнить первичную обработку датасета средствами библиотеки **Pandas** и ответить на ключевые вопросы бизнеса.

---

## Часть 1. Предобработка данных (Data Cleaning)

Создайте Jupyter Notebook и выполните следующие шаги:
1. **Загрузка и первичный обзор:**
   - Считайте CSV-файл вашего варианта с помощью `pd.read_csv()`. Обратите внимание на разделитель (в выгрузке используется `;`).
   - Выведите первые 5 строк таблицы (`.head()`).
   - Изучите размерность таблицы (`.shape`) и типы данных (`.info()`, `.dtypes`).
2. **Поиск и удаление дубликатов:**
   - Найдите строки-дубликаты по уникальному идентификатору `session_id` с помощью метода `.duplicated()`.
   - Удалите дубликаты, оставив первые вхождения (`.drop_duplicates()`), и проверьте, как изменилось количество строк.
3. **Стандартизация категорий:**
   - В столбце `pc_zone` есть опечатки (лишние пробелы, разный регистр, например `" standard "`, `"ps5"`, `"VIP "`). Приведите значения к каноническому виду: `Standard`, `VIP`, `Bootcamp`, `PS5` (используйте строковые методы `.str.strip()`, `.replace()` или `.map()`).
   - В столбце `gender` встречаются различные обозначения (`"m"`, `"Мужской"`, `"1"`, `"ж"` и т.д.). Приведите все значения к двум единым категориям: `"M"` и `"F"`.
4. **Обработка пропусков:**
   - Проверьте количество пропущенных значений во всех столбцах (`.isna().sum()`).
   - В столбце `payment_method` заполните технические пропуски константой `"Unknown"`.
   - **Анализ рейтинга:** экран оценки после сессии является необязательным, поэтому многие клиенты пропускают отзыв.
     - Посчитайте, какая доля клиентов оставила оценку (`.notna().mean() * 100`).
     - Рассчитайте среднюю оценку среди тех, кто оставил отзыв.
     - Для подготовки данных к дальнейшим вычислениям заполните пропуски в `client_rating` средним значением.

---

## Часть 2. Анализ данных (Data Analysis)

На чистых данных ответьте на вопросы бизнеса:
1. **Анализ выручки и игровых зон:**
   - Добавьте новый расчетный столбец `total_revenue`, равный сумме аренды ПК (`total_paid`) и трат в баре (`extra_bar_spent`).
   - Какова общая выручка клуба за месяц?
   - Сгруппируйте данные по игровым зонам (`pc_zone`) и для каждой зоны рассчитайте:
     - Количество посещений (сессий);
     - Суммарную выручку;
     - Среднюю выручку на одну сессию (средний чек).
   - Какая зона лидирует по числу гостей, а какая приносит наибольший средний чек?
2. **Игровые предпочтения:**
   - Выведите топ-3 самых популярных игр (`game_played`) по количеству сыгранных сессий (`.value_counts()`).
3. **Аудитория и удовлетворенность:**
   - Посчитайте процентное соотношение пользователей по полу (`gender`).
   - Какая средняя оценка (`client_rating`) у клиентов разного пола и в разных игровых зонах (`pc_zone`)?
4. **Экспорт результата:**
   - Сохраните очищенный датафрейм в новый CSV-файл без сохранения индекса (`.to_csv('cleaned_dataset.csv', index=False, sep=';')`).